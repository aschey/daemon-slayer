[package]
edition = "2021"
name = "daemon-slayer-tray"
version = "0.1.0"

[dependencies]
daemon-slayer-client = { path = "../daemon-slayer-client" }
image = "0.24"
tao = { version = "0.15", features = ["tray"] }
