[package]
edition = "2021"
name = "integration-tests"
publish = false
version = "0.1.0"

[dependencies]
async-trait = "0.1.58"
axum = "0.6.1"
confique = "0.2.2"
daemon-slayer = {path = "../crates/daemon-slayer", features = ["server-standard", "client-standard", "extras"]}
futures = "0.3.25"
reqwest = {version = "0.11.12", features = ["blocking"]}
serde = "1.0.147"
tokio = {version = "1.21.2", features = ["rt-multi-thread"]}
tracing = "0.1.37"

[dev-dependencies]
assert_cmd = "2.0.6"
